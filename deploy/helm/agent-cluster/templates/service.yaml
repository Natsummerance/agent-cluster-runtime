apiVersion: v1
kind: Service
metadata:
  name: {{ include "agent-cluster.fullname" . }}
  labels:
    {{- include "agent-cluster.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "agent-cluster.selectorLabels" . | nindent 4 }}
