# agent-cluster-runtime v0.6.0 续跑交接（13.11 已完成 -> 13.12~13.16 待做）

> 写给下一个 Codex 对话。读完本文件 + 下方「必读文档」即可直接续跑，无需重新探查。
> 生成时间 2026-08-13 (Asia/Shanghai)。仓库绝对路径 `T:\Programming\Project\codex\agent\agent-cluster-runtime`。

## 0. 一句话现状

Task 13.1~13.11 已全部完成并提交，当前 HEAD `1bdc529`；工作树干净；**未 push**（本地未配置 remote / 未配 GITHUB_TOKEN）。
剩余：13.12（桌面打包矩阵+自动更新）、13.13（Docker 安装+doctor）、13.14（CI+Actions 模板）、13.15（真实后端 e2e）、13.16（验收+发布）。

## 1. 已完成提交（新 -> 旧）

- 1bdc529 Task 13.11: full i18n en-US/zh-CN with i18n:check
- 66d49ea Task 13.10: three-axis dashboard, task panel and resilient SSE client
- 74bb230 Task 13.9: realtime stdin injection via serve endpoint, panel and CLI
- 16a709c Task 13.8: stdlib RFC 6455 WebSocket channel with REST fallback
- e4dc5f5 Task 13.7 followup: retry transient project.json reads and assert fork error body
- c4cfe9b Task 13.7: REST/SSE extension with error codes and session.end sentinel
- d9d76d8 Task 13.6: auto-reviewer gate policy with human escalation
- f507687 Task 13.5: SessionManager with default worktree isolation and clean shutdown
- e1941a3 Task 13.4: fork-session with terminal-only source and checkpoint copy
- fbefc89 Task 13.3: project budget pool with hysteresis and unlock approval
- 9507763 Task 13.2: v0.5 session migration into project directory store
- f0eba6c Task 13.1: project-layer data models and ProjectStore

## 2. Task 13.11 完成证据（当次新鲜运行）

- 后端 `uv run pytest -q`：633 passed（169s）
- 前端 `npm run build`：green（3208 modules）
- 前端 `npm test`：130 passed / 18 files（125 基线 + 5 新 i18n 用例）
- 前端 `npm run e2e`：57 passed（56 基线 + 1 语言切换用例）
- `npm run i18n:check`：en-US <-> zh-CN key sets match (284 keys)

## 3. 环境与操作约束（重要，务必遵守）

- `apply_patch` 报 `Access is denied`（Z: 盘/权限）=> 用 PowerShell here-string + `uv run python -` 写文件；内容含换行时在 Python 里用 `\n`；文件写 UTF-8 无 BOM、LF（`newline="\n"`）。
- 单条 shell 命令过长报 `InvalidFilename (os error 206)` => 长 here-string 必须拆成多条命令。
- **不派发子智能体**（用户明确要求，子智能体无上下文太耗 token）；主线程直改。仅当主线程上下文退化才开子智能体，且需向用户说明。
- 模型路由：简单任务可切 deepseek-v4-flash、需思考用 deepseek-v4-pro（cc-switch）；但当前会话无 route 工具，一律现有模型直做。
- 流程：TDD（RED->GREEN）+ `verification-before-completion`——每条完成声明必须附当次运行的新鲜证据输出。
- 提交前缀 `Task 13.N:`，每任务单独 commit；版本号 13.1~13.15 保持 0.5.0 不动，13.16 才同步 0.6.0。
- CRLF 警告是 git autocrlf 行为，无害。

## 4. 用户硬约束

- **前端严禁伪造/硬编码数据，必须全面接真实后端**；`frontend/e2e/mock-api.ts` 是既有的 Playwright 拦截式测试后端，合法。
- 竞品基线 `docs/superpowers/research/2026-08-13-v0.6-competitive-synthesis.md`；与设计文档冲突时以用户对设计文档的审阅意见为准。
- macOS/Linux 本轮只出构建物、不验收；Docker 只做安装自动化、不新增容器验收。

## 5. 关键代码模式

- i18n：页面/组件用 `const intl = useIntl();`（from `../i18n`）；文案 `intl.formatMessage({ id, defaultMessage }, values)`；代码注释保留中文；模块常量中文文案改为 `..._ID` key + render 时 `formatMessage`（见 EventTimeline/Dashboard 的 AXIS_STATUS_ID）。
- `I18nProvider` 包在 `BrowserRouter` 外层、`ConfigProvider` 内层（App.tsx 已就位）。
- 错误文案：`appStore.apiErrorMessage(err)` 按 `errorCodeOf(err)` 查 `errors.<code>` 表，无 code/未知 code 原样展示后端文本。
- 组件测试 helper：`src/test/renderWithIntl.tsx`（默认 locale zh-CN）——需要 intl 的组件测试用 `renderWithIntl(<X />)`。
- Playwright AntD 下拉无 role=option：`page.locator('.ant-select-item-option', { hasText: 'en-US' }).click()`；Vitest 里点 `select.querySelector('.ant-select-selector')` 后 `findByTitle('en-US')`。
- 消息 JSON 在 `frontend/src/i18n/messages/{en-US,zh-CN}.json`（284 keys）；加 key 后必须 `npm run i18n:check` 校对；key 命名 `<page>.<component>.<token>`，`errors.*` 例外用 snake_case 对齐后端 code。

## 6. 必读文档（相对仓库根）

- `docs/superpowers/plans/2026-08-13-v0.6-implementation.md`（任务分解/每任务门槛/完成标准，13.12~13.16 在 468~640 行附近）
- `docs/superpowers/specs/2026-08-13-v0.6-design.md`（唯一结论设计，§13 打包矩阵、§14 CI、§16 收尾验收）
- `docs/superpowers/research/2026-08-13-v0.6-competitive-synthesis.md`（竞品基线）
- `docs/superpowers/handoff/2026-08-12-v0.5-to-v0.6.md`（上一轮交接，了解既有架构）

## 7. 并行/顺序提示

- 批次 A 可并行（写集两两不相交）：13.12（`desktop/**`）、13.13（`scripts/**` + `src/agent_cluster/{doctor.py,cli.py,server.py}` + `frontend/src/pages/Settings.tsx` + `tests/test_doctor.py`）、13.14（`.github/**` + `tests/test_ci_workflows.py`）。
- 13.15 依赖 13.6/13.7/13.9/13.10（均已满足）；13.16 必须最后。
- 用户要求非必要不派发子智能体，故建议主线程串行 13.12 -> 13.13 -> 13.14 -> 13.15 -> 13.16；如 13.12/13.13/13.14 三者写集不相交、可自行决定是否并行（但优先按用户 token 约束串行）。

## 8. 剩余任务完成标准（速查，详文见 plan 文档）

### 13.12 桌面打包矩阵与自动更新
- 文件：Modify `desktop/electron-builder.yml`、`desktop/package.json`（dep electron-updater；scripts build:win(x64+arm64)/build:mac/build:linux）、`desktop/main.js`、`desktop/preload.js`、`desktop/sign.ps1`；Create `desktop/updater.js`、`desktop/notarize.cjs`、`desktop/scripts/patch-update-metadata.js`。
- 要点：win nsis x64+arm64；mac dmg+zip arm64+x64；linux deb x64；afterSign 仅 NOTARIZE=1 执行，无证书跳过并产物名追加 -unsigned、latest-mac.yml 标 unsigned:true；publish github；updater 双通道 stable/latest + 失败回退 lastKnownGood + 版本钉扎（自研 10 行级扁平 YAML 子集解析 minimumVersion）+ main.js 启动看门狗 boot-ok；preload 最小 IPC（get/setUpdateSettings、checkForUpdates、onUpdateStatus）。
- 门槛：`node --check` 5 文件通过；`cd desktop && npm install && npm run pack`（x64 dir）与 `npx electron-builder --win nsis --arm64 --dir`（arm64 dir）成功且 win-unpacked 存在；`npx electron-builder --config desktop/electron-builder.yml --help` 可解析；patch-update-metadata.js 样例注入成功；回归 pytest + frontend build/test/e2e 全绿。
- 提交：`git add desktop/ && git commit -m "Task 13.12: desktop packaging matrix and electron-updater dual channel"`

### 13.13 Docker 自动安装 + doctor 联动
- 文件：Create `scripts/install-docker.ps1`、`scripts/install-docker.sh`、`scripts/linux-docker-commands.md`；Modify `src/agent_cluster/doctor.py`（CheckResult.action；run_doctor fix_docker/script_runner 可注入）、`src/agent_cluster/cli.py`（doctor --fix-docker）、`src/agent_cluster/server.py`（GET /api/v1/doctor、POST /api/v1/doctor/fix-docker 两新端点，复用 X-Auth-Token + 127.0.0.1，fix 只跑白名单 `scripts/install-docker.*`）、`frontend/src/pages/Settings.tsx`（环境卡片，走 13.11 i18n key）、`frontend/src/test/Settings.test.tsx`、`tests/test_doctor.py`。
- 门槛：test_doctor 4 用例（action 字段/runner 注入顺序/idempotent/endpoints 401）；ps1 用 PowerShell Parser 语法门、`bash -n`（bash 不存在跳过）；前端回归 + pytest 全绿。
- 提交：`git add scripts src/agent_cluster/doctor.py src/agent_cluster/cli.py src/agent_cluster/server.py frontend/src/pages/Settings.tsx frontend/src/test/Settings.test.tsx tests/test_doctor.py && git commit -m "Task 13.13: docker auto-install scripts with doctor --fix-docker"`

### 13.14 CI 工作流与 Actions 模板
- 文件：Create `.github/workflows/ci.yml`、`.github/workflow-templates/agent-delivery.yml`、`.github/workflow-templates/README.md`；Test `tests/test_ci_workflows.py`。
- 要点：ci.yml jobs = backend-test（uv sync --frozen + pytest）、frontend-test（npm ci + build + test + i18n:check）、e2e-real（needs backend-test，serve 后台 & + $! 记录 PID + finally kill）、package（仅 tag v*，matrix windows/macos-14 arm64/macos-13 x64/ubuntu，env GITHUB_TOKEN:'' 置空）、release（仅 tag v*，needs package，softprops/action-gh-release@v2，先跑 patch-update-metadata.js 注入 minimumVersion/unsigned）；模板 build(workflow_dispatch 输入 goal/model/budget)->environment: production 人工审批->ci(workflow_call 复用)->失败停在评论，**不自动合并**（无 merge/push-to-main）。
- 门槛：test_ci_workflows.py 6 个结构断言全过（jobs 存在/GITHUB_TOKEN 空/release needs+tag gate/e2e-real finally kill/模板 human approval+no merge/模板 workflow_call）；`uv run pytest -q` 全绿。
- 提交：`git add .github tests/test_ci_workflows.py && git commit -m "Task 13.14: CI workflows and first-party agent-delivery template"`

### 13.15 Playwright 真实后端 e2e（e2e:real，deterministic）
- 文件：Create `frontend/playwright.real.config.ts`、`frontend/e2e-real/*.spec.ts`（10 个）、`frontend/e2e-real/helpers/api.ts`；Modify `frontend/package.json`（scripts 加 `"e2e:real": "playwright test --config playwright.real.config.ts"`）。
- 要点：config testDir ./e2e-real、workers 1、fullyParallel false、baseURL http://127.0.0.1:8765；webServer command `uv run agent-cluster serve --port 8765 --auth-token ci`，url /api/v1/status，reuseExistingServer false，timeout 90_000（Playwright 自管启停、结束强制终止验证 13.5 无孤儿）。
- 10 个 spec（串行，断言必须落到具体字段值，禁止弱断言）：status / projects（含 v0.5 迁移生效）/ sessions（deterministic 至 completed，恢复+409）/ gates（auto accept + review.auto_decision）/ ws（原生 WS subscribe/snapshot/ping-pong/cancel）/ stdin（挂起注入作答 + transcript）/ fork（终态派生+血缘+账本不双计）/ budget（预警/硬上限 409/解锁 200 与 202/审批）/ dashboard-tasks（三轴契约+过滤/指派）/ audit（含新事件类型）。
- 门槛：`cd frontend && npm run e2e:real` 全绿（10 spec 0 failed）；`npm run e2e`（mock 57 条）与 `npm test` 不回归；`uv run pytest -q` 全绿。
- 提交：`git add frontend/playwright.real.config.ts frontend/e2e-real frontend/package.json && git commit -m "Task 13.15: real-backend Playwright e2e suite (deterministic)"`

### 13.16 端到端验收与发布
- 文件：Modify `pyproject.toml`、`src/agent_cluster/__init__.py`、`frontend/package.json`、`desktop/package.json`（版本号 0.5.0 -> 0.6.0 四文件同步）；Modify `README.md`、`PRODUCT.md`、`MANUAL.md`；Create `tests/test_t13_16.py`（in-process serve 全链路验收）、`docs/superpowers/handoff/2026-08-13-v0.6-to-v0.7.md`（v0.7 交接）。
- 顺序（D26 写死）：验收 -> 前端 build 复制进 desktop/dist-frontend -> Electron smoke -> NSIS -> tag v0.6.0 -> GitHub Release。
- test_t13_16.py 用例：in-process serve 随机端口：建项目（v0.5 迁移 fixture）-> deterministic 至 completed（review.auto_decision 在列）-> stdin 落 transcript/PRD -> fork 终态派生 -> 预算预警/unlock 审批 -> dashboard/tasks 契约 -> audit 含 session.end/session.forked/stdin.applied/review.auto_decision/budget.* -> SSE 哨兵重放；全部按字段机械判定。
- 门槛：`uv run pytest -q` 全绿（含 test_t13_16）；`cd frontend && npm run build && npm test && npm run e2e && npm run e2e:real && npm run i18n:check` 全绿；`node --check desktop/main.js desktop/preload.js desktop/updater.js`；NSIS 产物名含 0.6.0 + 本机安装/Electron smoke 人工 checklist。
- 提交：`git add -A && git commit -m "Task 13.16: v0.6.0 release - acceptance, version sync, installer and tag"`；tag `v0.6.0` push（内联 GITHUB_TOKEN）+ GitHub Release 资产齐全（x64+arm64 NSIS、mac dmg/zip -unsigned、linux deb、latest*.yml）。

## 9. 建议续跑第一步

1. `cd T:\Programming\Project\codex\agent\agent-cluster-runtime; git log --oneline -3` 确认 HEAD=1bdc529、`git status --short` 干净。
2. 读 `docs/superpowers/plans/2026-08-13-v0.6-implementation.md` 的 13.12~13.16 全量原文（本文件是速查）。
3. 从 13.12 开始按顺序执行；每任务跑完门槛后单独 commit。
4. push/Release 需内联 GITHUB_TOKEN；本地无 remote 时跳过 push 并在提交信息备注（与 v0.5 一致）。

## 10. 注意

- 本文件为续跑交接，勿与 13.16 要产出的 `2026-08-13-v0.6-to-v0.7.md`（版本收尾交接）混淆。
- 13.12 只读 frontend/dist（消费 13.11 最终产物），不动前端源码。
- 13.13 与 13.11 都写 Settings.tsx，但 13.11 已完成提交，故现在无冲突。
