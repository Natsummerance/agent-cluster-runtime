# agent-cluster-runtime v0.6.4 维护线交接（Task 13.18–13.20）

> 写给下一个 Codex 对话：读完本文件 + §8 必读清单即可直接续跑，无需重新探查仓库。
> 生成时间 2026-08-13 (Asia/Shanghai)。仓库 `T:\Programming\Project\codex\agent\agent-cluster-runtime`。
> 当前 HEAD=`36f4a8d`（main 已推送）；tag `v0.6.4` → `d0eec78`；`v0.6.3` → `efe8135` 未移动；工作树干净（根 `logs/` 已 gitignore）。

## 0. 一句话现状

v0.6.2 已发布、v0.6.3/v0.6.4 已重发（CORS 修复、React Router v7、前端代码分割；tag run 全绿、Release 各 18 资产已核验）。维护线 Task 13.18/13.19/13.20 完成：
README 全面美化（LOGO/徽章/特性表/目录锚点修复）、前端 LOGO 集成（favicon + Sider）、
serve CORS（浏览器/桌面可直连后端）、进化页修复（API 信封解包）、集成页信封解包、
dashboard/tasks/budget 旧项目空态、antd 弃用警告清理、经验库 4 条新增。
本地 dev 正在运行：后端 `127.0.0.1:8765`、前端 `127.0.0.1:5173`（重启见 §3）。

## 1. 本次会话完成清单（Task 13.18 维护线，自 61a49a3 起）

| SHA | 内容 |
|---|---|
| `61a49a3` | README 美化（LOGO/徽章/TOC/特性表/emoji 标题 + 2 个 U+FE0F 锚点修复）；`docs/logo.svg`；`frontend/public/logo.svg`；`index.html` favicon；`AppLayout` Sider LOGO |
| `743ca07` | `server.py` CORS：`_send_json`/SSE 加 `Access-Control-Allow-Origin: *` + `do_OPTIONS` 预检；`test_t12_11.py` +2 测试 |
| `c7a54a9` | `lessons/05-frontend` 记录 CORS 坑（含 Playwright 勿用 networkidle，SSE 永不 idle） |
| `a6293bb` | 前端信封解包：`fetchEvolutionProposals` 返回 `data.proposals`；`fetchPlugins/Skills/Mcp` 解包 `data.<key>`；测试修正为真实信封结构 +2 |
| `422bd7e` | `_handle_dashboard`：索引在→空三轴 200，索引无→404；+2 测试 |
| `00b508f` | `lessons/05-frontend` 记录信封契约 + 旧项目 dashboard 坑 |
| `43b3ed5` | `task_entries`（索引检查+空列表兜底）、`_handle_budget`（默认预算结构兜底）；+4 测试 |
| `169875f` | antd `destroyOnClose`→`destroyOnHidden`（`Projects.tsx`/`ProjectSessions.tsx`） |
| `efe8135` | v0.6.3 版本同步（12 处清单 + uv.lock）+ Release 重发（tag run `31714779151` 全绿、18 资产核验通过） |
| `dde6309` | 前端：react-router-dom 升 v7.18.2（消除 future-flag 警告）+ 路由懒加载（9 页 React.lazy + Suspense）+ vendor 拆分（react/antd/intl/misc，阈值 1200） |
| `c2ef7b8` | 前端：恢复 fetchMcp IntegrationNote 契约 + e2e mock 信封对齐（a6293bb 遗留回归，e2e mock 5 条转绿） |
| `8227336` | CI package job 自建前端并 stage `desktop/dist-frontend`（修复安装包缺 UI 缺口） |
| `d0eec78` | v0.6.4 版本同步（12 处清单 + uv.lock）+ Release 重发（tag run `31754878683` 全绿、18 资产核验通过） |
| `36f4a8d` | CI release job `download-artifact` 限定 `pattern: package-*`（防 diagnostics 垃圾资产） |

**验证证据（最后一次全跑）**：`uv run pytest -q` → 658 passed（~3min）；`npx vitest run` → 133 passed；
`npx tsc --noEmit` 0；`npm run build` 绿（dist 含 logo.svg）；无头浏览器 10 路由：0 console 错误、0 HTTP≥400。

## 2. 待办（按优先级）

1. ~~桌面版安装包缺 CORS 修复~~ **已完成**：v0.6.3 重发（2026-08-13）。
2. ~~React Router v7 future-flag 警告~~ **已完成**：v0.6.4 升级 `react-router-dom` 7.18.2（2026-08-14）。
3. ~~vite chunk 过大（1.37MB）~~ **已完成**：v0.6.4 路由懒加载 + vendor 拆分，index 1370KB → 48KB（2026-08-14）。
4. v0.7 主史诗：见 §7。

## 3. 本地运行状态（本次会话已起）

- 后端：`.venv\Scripts\python.exe -m agent_cluster.cli serve` → `127.0.0.1:8765`（日志 `logs/serve.log`、`logs/serve.err.log`）。
- 前端：`cd frontend; npm run dev` → `127.0.0.1:5173`（日志 `logs/vite.log`、`logs/vite.err.log`）。
- 重启后端：先杀 8765 监听 PID（`Get-NetTCPConnection -LocalPort 8765 -State Listen`），再 Start-Process（`-WindowStyle Hidden`）。
  **坑**：启动命令与探活必须拆两条 PowerShell 命令（合并会被策略拦）。
- 无头页面验证模板：脚本须放 `frontend/` 下（才能 import playwright），收集 console/pageerror/HTTP≥400，
  用 `domcontentloaded` 勿用 `networkidle`（SSE 长连接）。
- 停服：杀 8765 与 5173 监听 PID。

## 4. 文件目录索引（按图索骥，无需翻文件）

**后端 `src/agent_cluster/`**
- `cli.py` — CLI 入口（serve/demo/proposals 等子命令）。
- `server.py` — serve 后端（stdlib ThreadingHTTPServer）：`do_GET/do_POST/do_OPTIONS` 路由、`_handle_*`、
  `WorkbenchServer`（`index` 全局索引 / `_project_store` ProjectStore / `manager` SessionManager）、
  `project_workspace()` 走索引、`_package_version()` 开发回退 `0.6.2-dev`。
- `projects.py` — ProjectStore：`get()` 缺失抛 KeyError、`budget_status()`、`aggregate_used_tokens()`。
- `evolution.py` + `evolution_integration.py` — 六步进化引擎 + 桥（`evolution_bridge()` 走索引 workspace）。
- `metrics.py` / `ledger.py` / `memory.py`（MemoryStore 在 projects.py:675 工厂）。
- 版本文件：`pyproject.toml` + `src/agent_cluster/__init__.py` = 0.6.4。

**前端 `frontend/`**
- `src/App.tsx` — 10 条路由（`/`、`/projects`、`/projects/:pid/sessions[/:sid]`、`/artifacts`、`/memory`、`/evolution`、`/integrations`、`/audit`、`/settings`）。
- `src/api/client.ts` — fetch 封装：`apiRequest` 解 `{ok,data}` 信封返回 `data`；`DEFAULT_BASE_URL=http://127.0.0.1:8765`；`configureApi` 注入 baseUrl/authToken。
- `src/api/endpoints.ts` — 端点函数；**信封解包约定见 §5**。
- `src/api/types.ts` — 类型（`EvolutionProposal`、`IntegrationNote` 等）。
- `src/pages/*.tsx` — 页面；`src/layout/AppLayout.tsx` — Sider LOGO（`public/logo.svg`）。
- `src/i18n/`、`src/store/appStore.ts`、`src/hooks/useProjectParam.ts`、`src/test/`（18 个测试文件，`renderWithIntl.tsx` 必用）。
- `public/logo.svg` — favicon + 侧栏 LOGO 资源。
- `e2e/mock-api.ts`（拦截式测试合法）、`e2e-real/`（真实后端 16 条，`playwright.real.config.ts`）。

**桌面 `desktop/`**
- `main.js` — 后端 spawn/端口反查进程树清理/`--smoke`/`backendUrl` 注入；`preload.js` — `window.agentCluster.getBackendUrl()`；
  `updater.js` — electron-updater 双通道；`electron-builder.yml`；`package.json` = 0.6.4。

**文档/工具**
- `README.md`（已美化，含 Release 链接）、`docs/logo.svg`（官方 LOGO 1024²）、`docs/MANUAL.md`、`docs/PRODUCT.md`。
- `docs/lessons/README.md` — 经验库索引（8 模块按需加载）；`docs/LEARNINGS.md` — 总纲。
- `docs/superpowers/plans/2026-08-13-v0.6-implementation.md`（83KB 主计划）、`implementation-plan.md`。
- `docs/superpowers/handoff/` — 本文件为最新一份（AGENTS.md 规定只读最新）。
- `scripts/troubleshoot.ps1` — 一键诊断（端口/进程/缓存/venv/CI，只读）。
- `.github/workflows/ci.yml` — 五段 CI + 失败时 `diagnostics-*` artifact（保留 1 天）。

## 5. 关键契约（写代码/测试前必看）

- **API 信封**：响应 `{ok, data}`；`data` 常为对象（`proposals`/`plugins`/`skills`/`mcp`/budget/三轴 dashboard）。
  前端端点函数必须解包后返回数组/结构；测试 mock 用真实信封（`{ok:true,data:{proposals:[...]}}`），
  用 `data: []` 会掩盖契约不匹配（antd Table 收到对象即崩：`rawData.some is not a function`）。
- **CORS**：`server.py` 已全局 `ACAO: *` + `do_OPTIONS`（允许 `X-Auth-Token`/`Content-Type`）+ SSE 也带 ACAO。
  浏览器 dev（5173）与桌面（file://）直连 8765 均可用。
- **旧项目语义**（v0.6 T13.5 双写前：全局索引有、ProjectStore 无）：
  `dashboard`→空三轴 200；`tasks`→空列表 200；`budget`→默认预算结构 200；`workspace/memory/evolution`→走索引正常。
  完全不存在 → 404 `not_found`。新增项目级端点遵循同样兜底。
- **workspace 解析**：`project_workspace()` 优先全局索引 entry 的 workspace 字段。
- **版本断言**：`_package_version()` 开发回退 `0.6.4-dev`；版本同步 12 处清单见 `docs/lessons/06-versioning.md`。
- **i18n**：key `<page>.<component>.<token>`；`errors.*` snake_case 对齐后端 code；测试用 `renderWithIntl`；
  AntD 下拉无 role=option → `.ant-select-item-option` + hasText。
- **测试**：`tests/test_t12_11.py` 的 `workbench` fixture（in-process serve）是后端端点测试标准写法；
  `ws.index.add_project(id,name,workspace)` 可构造"仅索引"旧项目场景。

## 6. 执行模式与约束（延续）

- 每任务独立 `git commit` + `git push`；前缀 `Task 13.18:`（v0.6.x 维护）/ `Task 14.N:`（v0.7）。
- 前端**严禁伪造/硬编码数据**，全面接真实后端；`frontend/e2e/mock-api.ts` 仅拦截式测试合法。
- 流程 TDD（RED→GREEN）+ verification-before-completion（完成声明附当次新鲜证据）。
- `apply_patch` 报 Access denied → `[System.IO.File]::WriteAllText`（UTF-8 无 BOM）。
- 单条命令过长/`Remove-Item` 被策略拦 → 拆多条 + .NET API；CRLF 警告无害。
- **不派发子智能体**（主线程直改）。
- **3 次即停**：同一问题修复 ≤3 次，第 3 次仍失败 → 停止 → `scripts/troubleshoot.ps1` 取证 → 对照经验库 → 书面根因才可再修。

## 7. v0.7 主史诗（来自 `2026-08-13-v0.6-to-v0.7.md`）

- 企业集成：SSO/AD/LDAP、团队与权限（RBAC）、审计合规导出。
- 云端化：托管部署（K8s/Helm）、多租户。
- OAuth MCP：MCP 服务器 OAuth 认证（rfc 8844）。
- 完整 PPM：组合层 API/看板已就绪，缺高级编排、资源日历、跨项目依赖图。

## 8. 必读文档（新对话开工顺序）

1. 本文件（现状/契约/索引）。
2. `docs/lessons/README.md`（索引表，按任务只加载对应模块，勿整库读）。
3. `docs/lessons/07-debugging.md`（任何修复尝试前）。
4. `docs/superpowers/plans/2026-08-13-v0.6-implementation.md`（按任务章节；v0.7 任务另有新 plan）。
5. `AGENTS.md`（加载协议 + 硬约束，开工即读）。

## 9. v0.6.3 重发实录（2026-08-13，Task 13.19）

- 内容：版本 0.6.2 → 0.6.3 全量同步（12 处清单精确替换 + `uv lock`；desktop lock 仅改行 3+9，未触碰 `iconv-lite ^0.6.2` 依赖），随 v0.6.3 发布 `743ca07` 之后的全部 9 个维护提交（CORS 修复等）。
- 提交：`efe8135`（Task 13.19: release - version bump）= tag `v0.6.3`；`v0.6.2` → `99b9633` 未移动。
- 验证：后端全量 `uv run pytest -q` 658 passed；前端 vitest 133 passed + `npm run build` 绿 + i18n 296 keys；运行后端 CORS 实测 GET 200 + `ACAO: *` + OPTIONS 预检 204。
- 桌面本地构建：`npx electron-builder --win nsis --x64 --publish never` → `AgentClusterWorkbench Setup 0.6.3 x64.exe`；`--smoke` 输出 SMOKE OK、无孤儿进程/端口残留。
- 发布：tag run `31714779151` 全绿（8 job）；Release v0.6.3 https://github.com/Natsummerance/agent-cluster-runtime/releases/tag/v0.6.3 —— 18 资产无重复、无 unpacked；三个 latest*.yml 均含 `minimumVersion: 0.6.3`，`latest-mac.yml` 含 `unsigned: true`。
- 经验：本地起 serve 必须用 `.venv\Scripts\python.exe`（uv 裸 cpython 无 agent_cluster 模块，`python -m agent_cluster.cli serve` 报 ModuleNotFoundError）。

## 10. v0.6.4 重发实录（2026-08-14，Task 13.20）

- 内容：① `react-router-dom` 6.30.4 → 7.18.2（消除 `v7_startTransition`/`v7_relativeSplatPath` future-flag 警告，导入路径不变）；② 前端代码分割：9 页 `React.lazy` + Suspense（Spin 无 tip），`manualChunks` 拆分 vendor-react/antd/intl/misc，`chunkSizeWarningLimit: 1200`，index 1370KB → 48KB、最大块 vendor-antd 1015KB；③ 版本 0.6.3 → 0.6.4（12 处清单 + uv.lock）。
- 修复的既有回归（a6293bb 遗留，非本次引入）：`fetchMcp` 恢复 IntegrationNote 契约（真后端 `data{stdio,http,note}` 无 `mcp` key）；e2e `mock-api.ts` 信封对齐（evolution → `data.proposals`、mcp → `data{stdio,http,note}`），e2e mock 57 条转绿（此前 5 条红）。
- **CI 缺口修复（重要）**：`desktop/dist-frontend` 被 gitignore，CI package job 此前不构建前端 → v0.6.2/v0.6.3 CI 安装包大概率无工作台 UI。v0.6.4 起 package job 自建前端并复制 dist-frontend；release job `download-artifact` 限定 `pattern: package-*`（首轮失败产生的 `pytest-output.log` 曾被当资产上传，已删并防复发）。
- 提交：`dde6309`/`c2ef7b8`/`8227336`/`d0eec78`（bump）= tag `v0.6.4`；`36f4a8d`（CI 资产过滤）。`v0.6.3` → `efe8135` 未移动。
- 验证：后端全量 658 passed（CI 首轮 `test_start_session_restore` 时序偶发，重跑 job 后全绿）；前端 tsc 0 + vitest 134 + build 绿（chunk 列表见上）+ i18n 296；运行后端 CORS GET 200 + `ACAO: *` + OPTIONS 204（version 0.6.4）；桌面本地构建 + `--smoke` OK，`app.asar` 含 `dist-frontend/index.html` 与新 chunk 资产（asar 路径分隔符为 `\`）。
- 发布：tag run `31754878683`（重跑 backend-test 后）8 job 全绿；Release v0.6.4 https://github.com/Natsummerance/agent-cluster-runtime/releases/tag/v0.6.4 —— 18 资产无重复、无 unpacked、无垃圾；三个 latest*.yml 均含 `minimumVersion: 0.6.4`，`latest-mac.yml` 含 `unsigned: true`。
